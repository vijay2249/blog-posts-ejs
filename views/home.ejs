<%- include('partials/header') %>
  <h1>Welcome to Home page</h1>
  <p><%= startContent %></p>

  <% data.posts.forEach(post =>{ %>
    <h2><%= post.title %></h2>
    <p>
      <%= post.content.substring(0, Math.min(post.content.length, 100))+"..." %>
      <a href="/posts/<%= post.title.replace(' ', '-') %>">Read More</a>
    </p>
  <% }) %>

<%- include('partials/footer') %>